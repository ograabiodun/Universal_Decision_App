{
  "name": "decision-audit-api",
  "version": "1.0.0",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "start": "npm run build && func start",
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "test": "vitest run"
  },
  "dependencies": {
    "@azure/cosmos": "^4.0.0",
    "@azure/functions": "^4.0.0",
    "openai": "^4.0.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}